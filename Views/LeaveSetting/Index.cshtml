@model WebENG.Models.UserModel;
@{
    ViewData["Title"] = "Leave Setting";
}
<link href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

<style>

    .custom-control-input:not(:checked) ~ .custom-control-label::before {
        background-color: lightgray;
        border-color: lightgray;
    }

    .custom-control-input:checked ~ .custom-control-label::before {
        background-color: green;
        border-color: green;
    }

    .kanit-thin {
        font-family: "Kanit", sans-serif !important;
        font-weight: 100 !important;
        font-style: normal !important;
    }

    .kanit-extralight {
        font-family: "Kanit", sans-serif;
        font-weight: 200;
        font-style: normal;
    }

    .kanit-light {
        font-family: "Kanit", sans-serif !important;
        font-weight: 300 !important;
        font-style: normal !important;
        color: dimgrey;
    }

    .kanit-regular {
        font-family: "Kanit", sans-serif;
        font-weight: 400;
        font-style: normal;
    }

    .kanit-medium {
        font-family: "Kanit", sans-serif;
        font-weight: 500;
        font-style: normal;
    }

    .kanit-semibold {
        font-family: "Kanit", sans-serif;
        font-weight: 600;
        font-style: normal;
    }

    .kanit-bold {
        font-family: "Kanit", sans-serif;
        font-weight: 700;
        font-style: normal;
    }

    .kanit-extrabold {
        font-family: "Kanit", sans-serif;
        font-weight: 800;
        font-style: normal;
    }

    .kanit-black {
        font-family: "Kanit", sans-serif;
        font-weight: 900;
        font-style: normal;
    }

    .kanit-thin-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 100;
        font-style: italic;
    }

    .kanit-extralight-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 200;
        font-style: italic;
    }

    .kanit-light-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 300;
        font-style: italic;
    }

    .kanit-regular-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 400;
        font-style: italic;
    }

    .kanit-medium-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 500;
        font-style: italic;
    }

    .kanit-semibold-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 600;
        font-style: italic;
    }

    .kanit-bold-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 700;
        font-style: italic;
    }

    .kanit-extrabold-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 800;
        font-style: italic;
    }

    .kanit-black-italic {
        font-family: "Kanit", sans-serif;
        font-weight: 900;
        font-style: italic;
    }
    /* สไตล์สำหรับ input */
    input[type="text"] {
        transition: opacity 0.3s ease, background-color 0.3s ease; /* แอนิเมชันนุ่มนวล */
    }
        /* input ตอน disable */
        input[type="text"]:disabled {
            cursor: not-allowed;
        }
        /* input ตอน enable */
        input[type="text"]:not(:disabled) {
            opacity: 1;
        }
    /* สไตล์สำหรับ input */
    input[type="number"] {
        transition: opacity 0.3s ease, background-color 0.3s ease; /* แอนิเมชันนุ่มนวล */
    }
        /* input ตอน disable */
        input[type="number"]:disabled {
            cursor: not-allowed;
        }
        /* input ตอน enable */
        input[type="number"]:not(:disabled) {
            opacity: 1;
        }

    /* สไตล์สำหรับปุ่ม leave_group */
    .leave-group-btn {
        transition: all 0.3s ease; /* เหมือน dropzone */
    }

        .leave-group-btn.active {
            background-color: #0056b3 !important; /* น้ำเงินเข้มเมื่อเลือก */
            color: white !important; /* ตัวอักษรขาว */
            border-color: #0056b3 !important; /* ขอบน้ำเงินเข้ม */
            box-shadow: 0 4px 8px rgba(0, 86, 179, 0.3); /* เงาเล็กน้อย */
        }

        .leave-group-btn:hover {
            background-color: #007bff; /* สีน้ำเงินปกติเมื่อ hover */
            color: white;
            transform: translateY(-2px); /* ยกขึ้นเหมือน dropzone */
        }

    /****************************************************/

    /* สไตล์สำหรับเงื่อนไข */
    .condition-group {
        padding: 5px 0; /* เพิ่ม padding รอบ ๆ เพื่อความโปร่ง */
    }

    .condition-row {
        margin-bottom: 0px; /* ช่องว่างระหว่างแถว */
        padding: 5px 0; /* เพิ่ม padding ในแถว */
        transition: all 0.3s ease; /* แอนิเมชันลื่นไหล */
        display: flex; /* ทำให้เป็น Flexbox container */
        align-items: center; /* จัดกึ่งกลางในแนวตั้ง */
    }

        .condition-row:hover {
            background: #f8f9fa; /* Highlight เหมือน dropzone */
        }

    /* สไตล์สำหรับสวิตช์ */
    .modern-switch {
        position: relative;
        display: inline-block;
        width: 50px;
        height: 28px;
        margin-right: 10px;
    }

        .modern-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .modern-switch .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            border-radius: 28px;
            transition: 0.3s;
        }

            .modern-switch .slider:before {
                position: absolute;
                content: "";
                height: 24px;
                width: 24px;
                left: 2px;
                bottom: 2px;
                background-color: white;
                border-radius: 50%;
                transition: 0.3s;

            }

        .modern-switch input:checked + .slider {
            background-color: #007bff;
        }

            .modern-switch input:checked + .slider:before {
                transform: translateX(22px);
            }

        .modern-switch:hover .slider {
            background-color: #b3d7ff;
        }

    /*************************************************/

    /* Modern Button Styles */
    .modern-btn {
        padding: 8px 0px;
        border-radius: 8px;
        font-size: 18px;
        transition: all 0.3s ease;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        border: none;
        width: 100%; /* เต็มความกว้างของ column */
        height: 100%;
        text-align: center;
    }

        .modern-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            color: white !important; /* ตัวอักษรสีขาวตอน hover */
        }

        .modern-btn:active {
            transform: translateY(0);
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

    /* Cancel Button */
    .btn-cancel {
        background-color: #dc3545; /* สีแดง Bootstrap */
        color: white;
    }

        .btn-cancel:hover {
            background-color: #c82333; /* แดงเข้มเมื่อ hover */
        }

    /* Save Button */
    .btn-save {
        background-color: #034694; /* สีน้ำเงินเดิม */
        color: white;
    }

        .btn-save:hover {
            background-color: #023670; /* น้ำเงินเข้มเมื่อ hover */
        }

    .icon-shadow {
        display: inline-block;
        padding: 15px;
        border-radius: 50%;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    }

    /* Add Button */
    .btn-add {
        background-color: #53cc67;
        color: white !important;
    }

        .btn-add:hover {
            background-color: #42a052; /* ฟ้าเข้มเมื่อ hover */
            color: white !important;
        }

    /* Delete Button */
    .delete-btn {
        transition: all 0.3s ease;
    }

        .delete-btn:hover {
            background: #dc3545;
            color: white;
            transform: translateY(-2px);
        }
</style>
<!-------------------------------------------------------------------------------------------------------------------->

<div class="row" style="row-gap:1px; padding:10px">
    <div class="col-12 kanit-bold">
        @* <span class="kanit-bold" style="font-size:24px;color:dimgrey">ประเภทการลา</span> 
        <hr style="border:solid;color:gray;border-width:1px" />*@
        <h4 style="letter-spacing:2px; padding:10px; padding-left:15px; color:white; border-top-left-radius:20px; border-top-right-radius:20px; background: #2178BE;
                    background: linear-gradient(90deg, rgba(33, 120, 190, 1) 0%, rgba(40, 71, 95, 1) 100%);">
            ประเภทการลา
        </h4>
    </div>

    <div class="col-xl-2 col-md-3 col-sm-12">
        <div class="card card-dark">
            <div class="card-body" style="height:760px; overflow-y:auto">
                <form>
                    <div class="form-group row">
                        <input type="button" id="addLeave" class="btn btn-outline-info form-control kanit-medium" style="border-radius:5px; height:40px; letter-spacing:1px" value="+ เพิ่มประเภทการลา" />
                    </div>
                    <div id="leave_group">

                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="col-xl-10 col-md-9 col-sm-12">
        <div class="card card-dark">
            <!-- เพิ่มส่วนนี้ใหม่ - nav-tabs แยกออกมา -->
            <form>
                <div class="card-header-tabs mb-0" style="padding-left:10px">
                <ul class="nav nav-tabs sticky-tabs">
                    <li class="nav-item">
                        <a class="nav-link active kanit-medium" style="letter-spacing:2px; font-size:18px" href="#Leave" data-toggle="tab">การลา</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link kanit-medium" style="letter-spacing:2px; font-size:18px" href="#Entitlement" data-toggle="tab">สิทธิ์การลา</a>
                    </li>
                </ul>
            </div>
            </form>
            <div class="card-body" style="height:660px; overflow-y:auto;">
                <form>
                    <div class="tab-content" style="padding-top:20px">
                        <div class="tab-pane fade show active" id="Leave">
                            <div class="container d-flex justify-content-center">
                                <div class="col-xl-8 col-md-8 col-sm-12">
                                    <div class="row">
                                        <div class="col-xl-6 col-md-6 col-sm-12">
                                            <div class="form-group ">
                                                <label class="kanit-light" style="color:dimgrey; font-size:18px">ชื่อประเภท (TH)</label>
                                                <input id="update_type_leave_th" type="text" class="form-control kanit-light" style="text-align:start; font-size:18px" />
                                            </div>
                                        </div>
                                        <div class="col-xl-6 col-md-6 col-sm-12">
                                            <div class="form-group ">
                                                <label class="kanit-light" style="color:dimgrey; font-size:18px">ชื่อประเภท (EN)</label>
                                                <input id="update_type_leave_en" type="text" class="form-control kanit-light" style="text-align:start; font-size:18px" />
                                            </div>
                                        </div>
                                    </div>
                                    <div class="row">
                                        <div class="col-xl-4 col-md-4 col-sm-12">
                                            <div class="form-group ">
                                                <label class="kanit-light" style="color:dimgrey; font-size:18px">รหัสการลา</label>
                                                <input id="update_type_leave_code" type="text" class="form-control" style="text-align:start; font-size:18px" />
                                            </div>
                                        </div>
                                        <div class="col-xl-8 col-md-8 col-sm-12">
                                            <div class="form-group">
                                                <label class="kanit-light" style="color:dimgrey; font-size:18px">รายละเอียด</label>
                                                <textarea id="update_leave_description" class="form-control kanit-light" style="text-align:start; font-size:18px" placeholder="รายละเอียดเพิ่มเติม" rows="1"></textarea>
                                            </div>
                                        </div>
                                    </div>
                                    <hr /> <!------------------------------------------------------------------------------------------------------->
                                    <div class="row">
                                        <div class="col-12">
                                            <div class="form-group condition-group">
                                                <label class="kanit-regular" style="font-size:20px; color: #555555; letter-spacing:1px">การขอลา</label>
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">เงื่อนไขการขอลา</span>
                                                    </div>
                                                    <div class="col-4 kanit-light d-flex justify-content-end">
                                                        <select id="update_leave_request_timing"class="form-control">
                                                            <option value="Both">ลาล่วงหน้าและย้อนหลังได้</option>
                                                            <option value="Future">ลาล่วงหน้า</option>
                                                            <option value="Past">ลาย้อนหลัง</option>
                                                        </select>
                                                    </div>
                                                </div>
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">เพศ</span>
                                                    </div>
                                                    <div class="col-4 kanit-light d-flex justify-content-end">
                                                        <select id="update_leave_gender" class="form-control">
                                                            <option value="Both">ลาได้ทุกเพศ</option>
                                                            <option value="Male">เพศชาย</option>
                                                            <option value="Femal">เพศหญิง</option>
                                                        </select>
                                                    </div>
                                                </div>
                                                <!-- ต้องแนบไฟล์ -->
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">ต้องแนบไฟล์</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="custom-control custom-switch">
                                                            @* <input type="checkbox" class="custom-control-input" id="update_leave_require_attachment">
                                                            <label class="custom-control-label" for="update_leave_require_attachment" style="transform:scale(1.5)">
                                                            </label> *@
                                                            <label class="modern-switch">
                                                                <input type="checkbox" id="update_leave_require_attachment">
                                                                <span class="slider"></span>
                                                            </label>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div id="div_leave_attachment_threshold_days" class="row condition-row" hidden>
                                                    <div class="col-8">
                                                        <span class="kanit-light">เมื่อลาตั้งแต่</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="input-group">
                                                            <input id="update_leave_attachment_threshold_days" type="number" class="form-control kanit-light" placeholder="#" value="3" step="1" min="1" max="5" style="text-align:end" />
                                                            <div class="input-group-append">
                                                                <span class="input-group-text kanit-light">วัน</span>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <!-- กำหนดลาต่อเนื่อง -->
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">กำหนดลาต่อเนื่อง</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="custom-control custom-switch">
                                                            <label class="modern-switch">
                                                                <input type="checkbox" id="update_leave_continuous">
                                                                <span class="slider"></span>
                                                            </label>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div id="div_leave_continuous_threshold_days" class="row condition-row" hidden>
                                                    <div class="col-8">
                                                        <span class="kanit-light">ลาติดต่อกันได้ไม่เกิน</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="input-group">
                                                            <input id="update_leave_continuous_threshold_days" type="number" class="form-control kanit-light" placeholder="#" value="3" step="1" min="1" max="5" style="text-align:end" />
                                                            <div class="input-group-append">
                                                                <span class="input-group-text kanit-light">วัน</span>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <!-- กำหนดการอนุมัติ 2 ชั้น -->
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">ต้องมีการอนุมัติ 2 ชั้น</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="custom-control custom-switch">
                                                            <label class="modern-switch">
                                                                <input type="checkbox" id="enable_two_steps_approve">
                                                                <span class="slider"></span>
                                                            </label>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div id="div_two_steps_approve_threshold_days" class="row condition-row" hidden>
                                                    <div class="col-8">
                                                        <span class="kanit-light">เมื่อลาติดต่อกันเกิน</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="input-group">
                                                            <input id="update_two_steps_approve_threshold_days" type="number" class="form-control kanit-light" placeholder="#" value="3" step="1" min="1" max="5" style="text-align:end" />
                                                            <div class="input-group-append">
                                                                <span class="input-group-text kanit-light">วัน</span>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <hr /> <!------------------------------------------------------------------------------------------------------->

                                    <div class="row">
                                        <div class="col-12">
                                            <div class="form-group condition-group">
                                                <label class="kanit-medium" style="font-size:20px; color: #555555; letter-spacing:1px">เงื่อนไข</label>
                                                <!-- แถวที่ 1 -->
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">ไม่ได้รับค่าจ้าง</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <label class="modern-switch">
                                                            <input type="checkbox" id="update_leave_is_unpaid">
                                                            <span class="slider"></span>
                                                        </label>
                                                    </div>
                                                </div>

                                                <!-- แถวที่ 2 -->
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">นับวันหยุดเป็นวันลา</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <label class="modern-switch">
                                                            <input type="checkbox" id="update_leave_count_holidays_as_leave">
                                                            <span class="slider"></span>
                                                        </label>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <hr /> <!------------------------------------------------------------------------------------------------------->

                                    <div class="row">
                                        <div class="col-12">
                                            <div class="form-group condition-group">
                                                <label class="kanit-regular" style="font-size:20px; color: #555; letter-spacing:1px">แก้ไขล่าสุด</label>
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">ชื่อ</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <label id="update_leave_edit_by"class="kanit-light">
                                                            
                                                        </label>
                                                    </div>
                                                </div>
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">เวลา</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <label id="update_leave_edit_at" class="kanit-light">   
                                                        </label>
                                                    </div>
                                                </div>
                                                <div class="row condition-row">
                                                    <div class="col-8">
                                                        <span class="kanit-light">เปิดใช้งาน</span>
                                                    </div>
                                                    <div class="col-4 d-flex justify-content-end">
                                                        <div class="custom-control custom-switch">
                                                            @* <input type="checkbox" class="custom-control-input" id="update_leave_is_active">
                                                            <label class="custom-control-label kanit-light" for="update_leave_is_active" style="transform:scale(1.2)">
                                                            </label> *@
                                                            <label class="modern-switch">
                                                                <input type="checkbox" id="update_leave_is_active">
                                                                <span class="slider"></span>
                                                            </label>
                                                        </div>
                                                    </div>
                                                </div>
                                                <hr>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-------------------------------------------------------------------------------------------------------------------------->

                        <!-- สิทธิ์การลา -->
                        <div class="tab-pane fade" id="Entitlement">
                            <div class="container d-flex justify-content-center" >
                                <div class="col-xl-12 col-md-12 col-sm-12">
                                    <div class="row">
                                        <div class="col-xl-12 col-md-12 col-sm-12">
                                            <div class="card collapsed-card">
                                                <div class="card-header" style=" background: #EEE;">
                                                    <div class="row">
                                                        <div class="col-xl-4 col-md-4 col-sm-12">
                                                            <span class="kanit-medium" style="color:#555; font-size:20px; letter-spacing:2px">ผู้ช่วยผู้จัดการขึ้นไป</span>
                                                        </div>
                                                        <div class="col-xl-8 col-md-8 col-sm-12 d-flex justify-content-end">
                                                            <div class="row">
                                                                <div class="col-6">
                                                                    <div class="card-tools">
                                                                        <button type="button" class="btn btn-tool" data-card-widget="collapse" style="color:#555 ; font-size:20px">
                                                                            <i class="fas fa-chevron-down"></i>
                                                                        </button>
                                                                    </div>
                                                                </div>
                                                            </div>

                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="card-body">
                                                    <div class="row" style="row-gap:1px">
                                                        <div class="col-12">
                                                            <div id="div_entitlement_manager">
                                                            </div>
                                                            <div class="row">
                                                                <div class="col-xl-1 col-md-1 col-sm-6">
                                                                    <input type="button" class="kanit-light btn modern-btn btn-add" onclick="EntitlementManagerClicked()" value="+ เพิ่ม" />
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-------------------------------------------------------------------------------------------------------------------------->
                                    <div class="row">
                                        <div class="col-xl-12 col-md-12 col-sm-12">
                                            <div class="card collapsed-card">
                                                <div class="card-header" style="background-color:#EEE">
                                                    <div class="row">
                                                        <div class="col-xl-4 col-md-4 col-sm-12">@* 
                                                            <span class="kanit-bold" style="color:dimgrey">พนักงานทั่วไป</span> *@
                                                            <span class="kanit-medium" style="color:#555555; font-size:20px; letter-spacing:2px">พนักงานทั่วไป</span>
                                                        </div>
                                                        <div class="col-xl-8 col-md-8 col-sm-12 d-flex justify-content-end">
                                                            <div class="row">         
                                                                <div class="col-6">
                                                                    <div class="card-tools">
                                                                        <button type="button" class="btn btn-tool" data-card-widget="collapse" style="color:#555 ; font-size:20px">
                                                                            <i class="fas fa-chevron-down"></i>
                                                                        </button>
                                                                    </div>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="card-body">
                                                    <div class="row" style="row-gap:1px">
                                                        <div class="col-12">
                                                            <div id="div_entitlement_operation">
                                                            </div>
                                                            <div class="row">
                                                                <div class="col-xl-1 col-md-1 col-sm-6">
                                                                    <input type="button" class="kanit-light btn modern-btn btn-add" onclick="EntitlementOperationClicked()" value="+ เพิ่ม" />
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>

            <!-- ปุ่มบันทึก & ยกเลิก (หลัก)-->
            <div class="card-footer" style="background-color:white;">
                <div class="row justify-content-center" style="row-gap: 10px;">
                    <div class="col-xl-2 col-lg-2 col-md-3 col-sm-4 col-5">
                        <button class="btn modern-btn btn-cancel kanit-medium"  type="button">ยกเลิก</button>
                    </div>
                    <div class="col-xl-2 col-lg-2 col-md-3 col-sm-4 col-5">
                        <button class="btn modern-btn btn-save kanit-medium" onclick="UpdateSave()" type="button">บันทึก</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal : เพิ่มประเภทการลา -->
<div class="modal fade" id="modal_leave" tabindex="-1" role="dialog" aria-hidden="true" style="overflow-y:hidden">
    <div class="modal modal-dialog modal-dialog-centered modal-sm" role="document">
        <div class="modal-content" style="border-radius: 10px; overflow: hidden;">
            <div class="modal-header" style="background: #34ba7c;
                                            background: linear-gradient(90deg, rgba(52, 186, 124, 1) 0%, rgba(0, 138, 112, 1) 100%); color: white; border-bottom: none">
                <h5 class="modal-title kanit-medium" style="color: white; font-size: 20px; letter-spacing: 1px;" id="modal_leave_title">เพิ่มประเภทการลา</h5>
                <button type="button" class="close" style="color: white;" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true" style="color: white;">&times;</span>
                </button>
            </div>
            <div class="modal-body mb-4" style="height:250px; color:#555555">
                <form>
                    <div class="row">
                        <div class="col-12">
                            <div class="form-group ">
                                <label class="kanit-light" style="color:dimgrey">รหัสการลา</label>
                                <input id="add_type_leave_code" type="text" class="form-control" style="text-align:start" />
                            </div>
                        </div>
                        <div class="col-12">
                            <div class="form-group ">
                                <label class="kanit-light" style="color:dimgrey">ชื่อประเภท (TH)</label>
                                <input id="add_type_leave_th" type="text" class="form-control" style="text-align:start" />
                            </div>
                        </div>
                        <div class="col-12">
                            <div class="form-group ">
                                <label class="kanit-light" style="color:dimgrey">ชื่อประเภท (EN)</label>
                                <input id="add_type_leave_en" type="text" class="form-control" style="text-align:start" />
                            </div>
                        </div>
                    </div>                       
                </form>
            </div>
            <div class="modal-footer d-flex">
                <div class="container d-flex justify-content-end">
                    <div class="col-xl-12 col-md-12 col-sm-12">
                        <div class="row">
                            @* <div class="col-6">
                                <button class="form-control kanit-light" style="background-color:red; color:white; border-radius:8px" data-dismiss="modal">
                                    ยกเลิก
                                </button>
                            </div>
                            <div class="col-6">
                                <button id="btn_add_save" class="form-control kanit-light" style="background-color:#034694; color:white; border-radius:8px">
                                    บันทึก
                                </button>
                            </div> *@
                            <div class="col-6">
                                <button class="btn modern-btn btn-cancel kanit-medium" style="background-color:red; color:white; border-radius:8px" data-dismiss="modal">
                                    ยกเลิก
                                </button>
                            </div>
                            <div class="col-6">
                                <button id="btn_add_save" class="btn modern-btn btn-save kanit-medium" style="background-color:#034694; color:white; border-radius:8px">
                                    บันทึก
                                </button>
                            </div>
                        </div>
                        @* <div class="modal-footer" style="border-top: none; padding: 15px 30px; background-color: #f8f9fa;">
                            <div class="row justify-content-center" style=" width: 100%;">
                                <div class="col-xl-4 col-md-4 col-sm-4">
                                    <button type="button" class="btn modern-btn btn-cancel kanit-medium" data-dismiss="modal">ยกเลิก</button>
                                </div>
                                <div class="col-xl-4 col-md-4 col-sm-4">
                                    <button type="button" class="btn modern-btn btn-save kanit-medium" id="confirm_ok">ยืนยัน</button>
                                </div>
                            </div>
                        </div> *@
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal : บันทึกการตั้งค่า-->
<div class="modal fade" id="modal_confirm" tabindex="-1" role="dialog" aria-labelledby="modal_confirm_title" aria-hidden="true">
    <div class="modal-dialog modal-md modal-dialog-centered"role="document">
        @* <div class="modal-content">
                <div class="modal-header text-white" style="background-color:#034694; font-size:18px; letter-spacing:1px">
                    <h5 class="modal-title kanit-regular" style="color:white; font-size:22px;" id="modal_confirm_title">บันทึกการตั้งค่า</h5>
                    <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="confirm_content" class="text-center">
                        <i class="fas fa-save mb-2" style="font-size:40px; color:#034694"></i>
                        <p class="kanit-light" style="font-size:20px">คุณต้องการบันทึกหรือไม่?</p>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">ยกเลิก</button>
                    <button type="button" class="btn btn-success" id="confirm_ok">ยืนยัน</button>
                </div>
        </div> *@
        <div class="modal-content kanit-regular" style="border-radius: 10px; overflow: hidden;">
            <div class="modal-header" style="background: linear-gradient(90deg, #034694 0%, #022f5b 100%); border-bottom: none; padding: 15px;">
                <h5 class="modal-title kanit-medium" style="color: white; font-size: 24px; letter-spacing: 1px;" id="modal_confirm_title">บันทึกการตั้งค่า</h5>
                <button type="button" class="close" style="color: white; font-size: 24px;" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" style="padding: 20px; text-align: center;">
                <div id="confirm_content">
                    <i class="fas fa-save icon-shadow mb-3" style="font-size: 40px; color: #034694;"></i>
                    <p class="kanit-light" style="font-size: 20px; color: #333; margin-bottom: 0;">คุณต้องการบันทึกหรือไม่?</p>
                </div>
            </div>
            <div class="modal-footer" style="border-top: none; padding: 15px 30px; background-color: #f8f9fa;">
                <div class="row justify-content-center" style=" width: 100%;">
                    <div class="col-xl-4 col-md-4 col-sm-4">
                        <button type="button" class="btn modern-btn btn-cancel kanit-medium" data-dismiss="modal">ยกเลิก</button>
                    </div>
                    <div class="col-xl-4 col-md-4 col-sm-4">
                        <button type="button" class="btn modern-btn btn-save kanit-medium" id="confirm_ok">ยืนยัน</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

        
<partial name="ModalSpin" />

@section Scripts
{
    <script type="text/javascript">
        let entitlements = [];
        let data_entitlement_manager = [];
        let entitlement_manager = 0;
        let data_entitlement_operation = [];
        let entitlement_operation = 0;
        let leaves = [];
        let last_leave_id = "";
        $(document).ready(async function () {
            await GetLeaveGroup();
        });

        $('#addLeave').on('click', function () {
            $('#modal_leave').modal();
        });

        $('#btn_add_save').on('click', async function () {
            let leave_code = $('#add_type_leave_code').val();
            let leave_th = $('#add_type_leave_th').val();
            let leave_en = $('#add_type_leave_en').val();
            if (leave_code.trim() !== "" && leave_code !== null && leave_th.trim() !== "" && leave_th !== null && leave_en.trim() !== "" && leave_en !== null) {
                await $.ajax({
                type: "POST",
                url: '@Url.Action("CreateLeaveGroup", "LeaveSetting")',
                contentType: 'application/x-www-form-urlencoded',
                    data: { leave_code, leave_th, leave_en},
                success: function (response) {
                    let message = response;
                    if (message === "Success") {
                        location.reload();
                    }
                }
            });
            } else {
                alert("กรุณากรอกข้อมูลให้ครบถ้วน");
            }

        });

        async function UpdateSave() {
            if (last_leave_id !== "") {
                $('#modal_confirm').modal();
            } else {
                alert('กรุณาเลือกประเภทการลา');
            }
        }
        $('#confirm_ok').on('click', async function () {
            await ConfirmUpdateSave();
        });

        async function ConfirmUpdateSave() {
            $('#SpinModal').modal();
            let leave_type_id = last_leave_id;
            let leave_name_th = $('#update_type_leave_th').val();
            let leave_name_en = $('#update_type_leave_en').val();
            let leave_type_code = $('#update_type_leave_code').val();
            let description = $('#update_leave_description').val();
            let request_timing = $('#update_leave_request_timing').val();
            let gender_restriction = $('#update_leave_gender').val();
            let attachment_required = $('#update_leave_require_attachment').prop('checked');
            let attachment_threshold_days = $('#update_leave_attachment_threshold_days').val();
            let is_unpaid = $('#update_leave_is_unpaid').prop('checked');
            let count_holidays_as_leave = $('#update_leave_count_holidays_as_leave').prop('checked');
            let updated_by = $('#update_leave_edit_by').text();
            let updated_at = $('#update_leave_edit_at').text();
            let is_active = $('#update_leave_is_active').prop('checked');
            if (leave_name_th.trim() !== "" &&
                leave_name_en.trim() !== "" &&
                leave_type_code.trim() !== "" &&
                data_entitlement_manager.length > 0 &&
                data_entitlement_operation.length > 0) {
                var leave = JSON.stringify({
                    "leave_type_id": leave_type_id,
                    "leave_name_th": leave_name_th,
                    "leave_name_en": leave_name_en,
                    "leave_type_code": leave_type_code,
                    "description": description,
                    "request_timing": request_timing,
                    "gender_restriction": gender_restriction,
                    "attachment_required": attachment_required,
                    "attachment_threshold_days": attachment_threshold_days,
                    "is_unpaid": is_unpaid,
                    "count_holidays_as_leave": count_holidays_as_leave,
                    "updated_by": updated_by,
                    "updated_at": updated_at,
                    "is_active": is_active
                });
                var entitlement_manager = JSON.stringify({
                    "entitlement": data_entitlement_manager
                });
                var entitlement_operation = JSON.stringify({
                    "entitlement": data_entitlement_operation
                });
                await $.ajax({
                    type: "PUT",
                    url: '@Url.Action("UpdateLeaveGroup", "LeaveSetting")',
                    contentType: 'application/x-www-form-urlencoded',
                    data: { leave, entitlement_manager, entitlement_operation },
                    success: function (response) {
                        let message = response;
                        if (message === "Success") {
                            location.reload();
                        }
                    }
                });
            } else {
                alert("กรุณากรอกข้อมูลให้ครบ");
            }
            $('#SpinModal').modal('hide');
        }
        async function GetLeaveGroup() {
            await $.ajax({
                type: "GET",
                url: '@Url.Action("GetLeaveGroup", "LeaveSetting")',
                contentType: 'application/x-www-form-urlencoded',
                data: {},
                success: function (response) {
                    leaves = response.leaves;
                    entitlements = response.entitlements;
                    GenerateLeaveGroup(leaves);
                }
            });
        }

        /* action เพิ่มประเภทการลา */
        function GenerateLeaveGroup(leaves) {
            var html = '';
            for (var i = 0; i < leaves.length; i++) {
                html += `<div class="form-group row">
                                       <button type="button" id="leave_group_${i}" class="btn btn-outline-primary form-control kanit-medium leave-group-btn"
                                       style="border-radius:7px; height:40px" onClick="LeaveGroupClicked('${leaves[i].leave_type_id}', this)">
                                    ${leaves[i].leave_name_th}
                               </button>
                         </div>`;
            }

            $('#leave_group').empty();
            $('#leave_group').append(html);
        }

        function LeaveGroupClicked(leave_type_id, button) {

            // ลบ class active จากทุกปุ่มก่อน
            const buttons = document.querySelectorAll('.leave-group-btn');
            buttons.forEach(btn => btn.classList.remove('active'));

            // เพิ่ม class active ให้ปุ่มที่ถูกคลิก
            button.classList.add('active');

            last_leave_id = leave_type_id;

            //Get Entitlement

            data_entitlement_manager = [];
            data_entitlement_operation = [];

            let datas_entitlement_manager = entitlements.filter(f => f.leave_type_id === last_leave_id && f.position === "Manager").map(m => m);
            let datas_entitlement_operation = entitlements.filter(f => f.leave_type_id === last_leave_id && f.position === "Operation").map(m => m);
            for (let i = 0; i < datas_entitlement_manager.length; i++) {
                data_entitlement_manager.push({
                    "id": entitlement_manager,
                    "start_age": datas_entitlement_manager[i].start_age,
                    "before_age": datas_entitlement_manager[i].before_age,
                    "days": datas_entitlement_manager[i].days_per_year,
                    "hours": datas_entitlement_manager[i].hours_per_year
                });
                entitlement_manager++;
            }

            for (let i = 0; i < datas_entitlement_operation.length; i++) {
                data_entitlement_operation.push({
                    "id": entitlement_operation,
                    "start_age": datas_entitlement_operation[i].start_age,
                    "before_age": datas_entitlement_operation[i].before_age,
                    "days": datas_entitlement_operation[i].days_per_year,
                    "hours": datas_entitlement_operation[i].hours_per_year
                });
                entitlement_operation++;
            }

            console.log(entitlements);

            let leave = leaves.filter(f => f.leave_type_id === leave_type_id).map(m => m)[0];
            $('#update_type_leave_th').val(leave.leave_name_th);
            $('#update_type_leave_en').val(leave.leave_name_en);
            $('#update_type_leave_code').val(leave.leave_type_code);
            $('#update_leave_description').val(leave.description);
            $('#update_leave_request_timing').val(leave.request_timing);
            $('#update_leave_gender').val(leave.gender_restriction);
            leave.attachment_required === true ? $('#update_leave_require_attachment').attr('checked', true) : $('#update_leave_require_attachment').attr('checked', false);

            let chk = $('#update_leave_require_attachment').prop('checked');
            if (chk === true) {
                $('#div_leave_attachment_threshold_days').attr('hidden', false);
            } else {
                $('#div_leave_attachment_threshold_days').attr('hidden', true);
                
            }
            $('#update_leave_attachment_threshold_days').val(leave.attachment_threshold_days);
            leave.is_unpaid === true ? $('#update_leave_is_unpaid').attr('checked', true) : $('#update_leave_is_unpaid').attr('checked', false);
            leave.count_holidays_as_leave === true ? $('#update_leave_count_holidays_as_leave').attr('checked', true) : $('#update_leave_count_holidays_as_leave').attr('checked', false);
            $('#update_leave_edit_by').text(leave.updated_by);
            $('#update_leave_edit_at').text(leave.updated_at.replace('T', ' '));
            leave.is_active === true ? $('#update_leave_is_active').attr('checked', true) : $('#update_leave_is_active').attr('checked', false);

            CreateEntitlementManager();
            CreateEntitlementOperation();
        }

        $('#update_leave_require_attachment').on('change', function () {
            let chk = $('#update_leave_require_attachment').prop('checked');
            if (chk === true) {
                $('#div_leave_attachment_threshold_days').attr('hidden', false);
                $('#update_leave_attachment_threshold_days').val(3);
            } else {
                $('#div_leave_attachment_threshold_days').attr('hidden', true);
                $('#update_leave_attachment_threshold_days').val(0);
            }
        });
        
        $('#update_leave_continuous').on('change', function () {
            let chk = $('#update_leave_continuous').prop('checked');
            if (chk === true) {
                $('#div_leave_continuous_threshold_days').attr('hidden', false);
                $('#update_leave_continuous_threshold_days').val(3);
            } else {
                $('#div_leave_continuous_threshold_days').attr('hidden', true);
                $('#update_leave_continuous_threshold_days').val(0);
            }
        });

        $('#enable_two_steps_approve').on('change', function () {
            let chk = $('#enable_two_steps_approve').prop('checked');
            if (chk === true) {
                $('#div_two_steps_approve_threshold_days').attr('hidden', false);
                $('#update_two_steps_approve_threshold_days').val(3);
            } else {
                $('#div_two_steps_approve_threshold_days').attr('hidden', true);
                $('#update_two_steps_approve_threshold_days').val(0);
            }
        });
        
        /*
         =============== Entitlement Manager ================
        */
        function EntitlementManagerClicked() {

            data_entitlement_manager.push({ "id": entitlement_manager, "start_age": 1, "before_age": 1, "days": 1, "hours": 0 });
            CreateEntitlementManager();
            entitlement_manager++;

        }
        function CreateEntitlementManager() {
            $('#div_entitlement_manager').empty();
            for (let i = 0; i < data_entitlement_manager.length; i++) {

                let html = `<div class="row">
                                                                            <div class="col-xl-3 col-md-3 col-sm-6">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">อายุงานตั้งแต่</label>
                                                                                        <div class="input-group">
                                                                                                <input id="entitlement_manager_start_age_amount${data_entitlement_manager[i].id}" type="number" onChange="EntitlementManagerStartAgeAmountChanged(${data_entitlement_manager[i].id})" class="form-control" placeholder="1" value="${data_entitlement_manager[i].start_age}" step="1" min="1" max="12" style="text-align:end" />
                                                                                            <div class="input-group-append">
                                                                                                    <select id = "entitlement_manager_start_age_select${data_entitlement_manager[i].id}" onChange="EntitlementManagerStartAgeSelectChanged(${data_entitlement_manager[i].id})" class="kanit-light form-control">
                                                                                                    <option value="month">เดือน</option>
                                                                                                    <option value="year" selected>ปี</option>
                                                                                                </select>
                                                                                            </div>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>
                                                                                <div class="col-xl-3 col-md-3 col-sm-6">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">แต่ไม่ถึง</label>
                                                                                        <div class="input-group">
                                                                                            <input id="entitlement_manager_before_age_amount${data_entitlement_manager[i].id}" type="number" onChange="EntitlementManagerBeforeAgeAmountChanged(${data_entitlement_manager[i].id})" class="form-control" placeholder="#" value="${data_entitlement_manager[i].before_age}" step="1" min="1" max="30" style="text-align:end" />
                                                                                            <div class="input-group-append">
                                                                                                <select id = "entitlement_manager_before_age_select${data_entitlement_manager[i].id}" onChange="EntitlementManagerBeforeAgeSelectChanged(${data_entitlement_manager[i].id})" class="kanit-light form-control" disabled>
                                                                                                    <option value="month">เดือน</option>
                                                                                                    <option value="year" selected>ปี</option>
                                                                                                </select>
                                                                                            </div>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>
                                                                                <div class="col-xl-3 col-md-3 col-sm-6">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">จำนวนวัน</label>
                                                                                        <div class="input-group">
                                                                                            <input id="entitlement_manager_day_amount${data_entitlement_manager[i].id}" type="number" onChange="EntitlementManagerDayAmountChanged(${data_entitlement_manager[i].id})" class="form-control" placeholder="#" value="${data_entitlement_manager[i].days}" step="1" min="1" max="16" style="text-align:end" />
                                                                                            <div class="input-group-append">
                                                                                                <span class="input-group-text kanit-light">วัน</span>
                                                                                            </div>
                                                                                        </div>
                                                                                        </div>
                                                                                    </div>

                                                                                <div class="col-xl-2 col-md-2 col-sm-5">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">จำนวนชั่วโมง</label>
                                                                                        <div class="input-group">
                                                                                            <input id="entitlement_manager_hour_amount${data_entitlement_manager[i].id}" type="number" onChange="EntitlementManagerHourAmountChanged(${data_entitlement_manager[i].id})" class="form-control" placeholder="1" value="${data_entitlement_manager[i].hours}" step="1" min="1" max="24" style="text-align:end" />
                                                                                        <div class="input-group-append">
                                                                                            <span class="input-group-text kanit-light">ชั่วโมง</span>
                                                                                        </div>
                                                                                    </div>
                                                                                        </div>
                                                                                </div>
                                                                                <div class="col-1">
                                                                                    <div class="form-group">
                                                                                                <label class="kanit-light d-flex justify-content-center" style="color:dimgrey">ยกเลิก</label>
                                                                                        <div class="input-group d-flex justify-content-center">
                                                                                                                 <button type="button" class="btn btn-outline-danger btn-sm delete-btn" style="border-radius: 50%; transform:scale(1.1);" onClick="EntitlementManagerDelete(${data_entitlement_manager[i].id})">
                                                                                                    <i class="fas fa-trash-alt"></i>
                                                                                                </button>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>
                                                                            </div>`


                $('#div_entitlement_manager').append(html);
            }
        }
        function EntitlementManagerDelete(id) {

            const index = data_entitlement_manager.findIndex(item => item.id === id);
            if (index !== -1) {
                data_entitlement_manager.splice(index, 1);
            }
            CreateEntitlementManager();
        }

        function EntitlementManagerStartAgeAmountChanged(id) {
            let tag = "#entitlement_manager_start_age_amount" + id;
            let tag_year = "#entitlement_manager_start_age_select" + id;
            let type = $(tag_year).val();
            let amount = parseInt($(tag).val());

            if (type === "month"){
                amount = (amount / 12.0).toFixed(1);
            }
            const index = data_entitlement_manager.findIndex(item => item.id === id);
            data_entitlement_manager[index].start_age = amount;

            console.log(data_entitlement_manager);
        }

        function EntitlementManagerStartAgeSelectChanged(id){
            let tag = "#entitlement_manager_before_age_amount" + id;
            let before_age_year = "#entitlement_manager_before_age_select" + id;
            let start_age_year = "#entitlement_manager_start_age_select" + id;
            let type_start_age = $(start_age_year).val();
            let type_before_age = $(before_age_year).val();
            let amount = parseInt($(tag).val());

            if (type_start_age === "month") {
                if (type_before_age === "month") {
                    amount = (amount / 12.0).toFixed(1);
                }
                $(before_age_year).attr('disabled', false);
            }
            if (type_start_age === "year") {
                $(before_age_year).attr('disabled', true);
                $(before_age_year).val(type_start_age);
            }

            const index = data_entitlement_manager.findIndex(item => item.id === id);
            data_entitlement_manager[index].start_age = amount;
            console.log(data_entitlement_manager);
        }

        function EntitlementManagerBeforeAgeAmountChanged(id) {
            let tag = "#entitlement_manager_before_age_amount" + id;
            let tag_year = "#entitlement_manager_before_age_select" + id;
            let type = $(tag_year).val();
            let amount = parseInt($(tag).val());

            if (type === "month") {
                amount = (amount / 12.0).toFixed(1);
            }
            const index = data_entitlement_manager.findIndex(item => item.id === id);
            data_entitlement_manager[index].before_age = amount;

            console.log(data_entitlement_manager);
        }

        function EntitlementManagerBeforeAgeSelectChanged(id) {
            let tag = "#entitlement_manager_before_age_amount" + id;
            let before_age_year = "#entitlement_manager_before_age_select" + id;
            let type_before_age = $(before_age_year).val();
            let amount = parseInt($(tag).val());

            if (type_before_age === "month") {
                amount = (amount / 12.0).toFixed(1);
            }

            const index = data_entitlement_manager.findIndex(item => item.id === id);
            data_entitlement_manager[index].before_age = amount;
            console.log(data_entitlement_manager);
        }

        function EntitlementManagerDayAmountChanged(id) {
            let tag = "#entitlement_manager_day_amount" + id;
            let amount = parseInt($(tag).val());
            const index = data_entitlement_manager.findIndex(item => item.id === id);
            data_entitlement_manager[index].days = amount;
            console.log(data_entitlement_manager);
        }
        function EntitlementManagerHourAmountChanged(id) {
            let tag = "#entitlement_manager_hour_amount" + id;
            let amount = parseInt($(tag).val());
            const index = data_entitlement_manager.findIndex(item => item.id === id);
            data_entitlement_manager[index].hours = amount;
            console.log(data_entitlement_manager);
        }

        /*
         =============== Entitlement Operation ================
        */
        function EntitlementOperationClicked() {

            data_entitlement_operation.push({ "id": entitlement_operation, "start_age": 1, "before_age": 1, "days": 1, "hours": 0 });
            CreateEntitlementOperation();
            entitlement_operation++;

        }

        function CreateEntitlementOperation() {
            $('#div_entitlement_operation').empty();
            for (let i = 0; i < data_entitlement_operation.length; i++) {

                let html = `<div class="row">
                                                                            <div class="col-xl-3 col-md-3 col-sm-6">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">อายุงานตั้งแต่</label>
                                                                                        <div class="input-group">
                                                                                                <input id="entitlement_operation_start_age_amount${data_entitlement_operation[i].id}" type="number" onChange="EntitlementOperationStartAgeAmountChanged(${data_entitlement_operation[i].id})" class="form-control" placeholder="1" value="${data_entitlement_operation[i].start_age}" step="1" min="1" max="12" style="text-align:end" />
                                                                                            <div class="input-group-append">
                                                                                                    <select id = "entitlement_operation_start_age_select${data_entitlement_operation[i].id}" onChange="EntitlementOperationStartAgeSelectChanged(${data_entitlement_operation[i].id})" class="kanit-light form-control">
                                                                                                    <option value="month">เดือน</option>
                                                                                                    <option value="year" selected>ปี</option>
                                                                                                </select>
                                                                                            </div>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>
                                                                                <div class="col-xl-3 col-md-3 col-sm-6">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">แต่ไม่ถึง</label>
                                                                                        <div class="input-group">
                                                                                            <input id="entitlement_operation_before_age_amount${data_entitlement_operation[i].id}" type="number" onChange="EntitlementOperationBeforeAgeAmountChanged(${data_entitlement_operation[i].id})" class="form-control" placeholder="#" value="${data_entitlement_operation[i].before_age}" step="1" min="1" max="30" style="text-align:end" />
                                                                                            <div class="input-group-append">
                                                                                                <select id = "entitlement_operation_before_age_select${data_entitlement_operation[i].id}" onChange="EntitlementOperationBeforeAgeSelectChanged(${data_entitlement_operation[i].id})" class="kanit-light form-control" disabled>
                                                                                                    <option value="month">เดือน</option>
                                                                                                    <option value="year" selected>ปี</option>
                                                                                                </select>
                                                                                            </div>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>
                                                                                <div class="col-xl-3 col-md-3 col-sm-6">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">จำนวนวัน</label>
                                                                                        <div class="input-group">
                                                                                            <input id="entitlement_operation_day_amount${data_entitlement_operation[i].id}" type="number" onChange="EntitlementOperationDayAmountChanged(${data_entitlement_operation[i].id})" class="form-control" placeholder="#" value="${data_entitlement_operation[i].days}" step="1" min="1" max="16" style="text-align:end" />
                                                                                            <div class="input-group-append">
                                                                                                <span class="input-group-text kanit-light">วัน</span>
                                                                                            </div>
                                                                                        </div>
                                                                                        </div>
                                                                                    </div>

                                                                                <div class="col-xl-2 col-md-2 col-sm-5">
                                                                                    <div class="form-group">
                                                                                        <label class="kanit-light" style="color:dimgrey">จำนวนชั่วโมง</label>
                                                                                        <div class="input-group">
                                                                                            <input id="entitlement_operation_hour_amount${data_entitlement_operation[i].id}" type="number" onChange="EntitlementOperationHourAmountChanged(${data_entitlement_operation[i].id})" class="form-control" placeholder="1" value="${data_entitlement_operation[i].hours}" step="1" min="1" max="24" style="text-align:end" />
                                                                                        <div class="input-group-append">
                                                                                            <span class="input-group-text kanit-light">ชั่วโมง</span>
                                                                                        </div>
                                                                                    </div>
                                                                                        </div>
                                                                                </div>
                                                                                <div class="col-1">
                                                                                    <div class="form-group">
                                                                                                <label class="kanit-light d-flex justify-content-center" style="color:dimgrey">ยกเลิก</label>
                                                                                        <div class="input-group d-flex justify-content-center">
                                                                                             <button type="button" class="btn btn-outline-danger btn-sm delete-btn" style="border-radius: 50%; transform:scale(1.1);" onClick="EntitlementOperationDelete(${data_entitlement_operation[i].id})">
                                                                                                 <i class="fas fa-trash-alt"></i>
                                                                                             </button>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>
                                                                            </div>`


                $('#div_entitlement_operation').append(html);
            }
        }
        function EntitlementOperationDelete(id) {

            const index = data_entitlement_operation.findIndex(item => item.id === id);
            if (index !== -1) {
                data_entitlement_operation.splice(index, 1);
            }
            CreateEntitlementOperation();
        }

        function EntitlementOperationStartAgeAmountChanged(id) {
            let tag = "#entitlement_operation_start_age_amount" + id;
            let tag_year = "#entitlement_operation_start_age_select" + id;
            let type = $(tag_year).val();
            let amount = parseInt($(tag).val());

            if (type === "month") {
                amount = (amount / 12.0).toFixed(1);
            }
            const index = data_entitlement_operation.findIndex(item => item.id === id);
            data_entitlement_operation[index].start_age = amount;

            console.log(data_entitlement_operation);
        }

        function EntitlementOperationStartAgeSelectChanged(id) {
            let tag = "#entitlement_operation_before_age_amount" + id;
            let before_age_year = "#entitlement_operation_before_age_select" + id;
            let start_age_year = "#entitlement_operation_start_age_select" + id;
            let type_start_age = $(start_age_year).val();
            let type_before_age = $(before_age_year).val();
            let amount = parseInt($(tag).val());

            if (type_start_age === "month") {
                if (type_before_age === "month") {
                    amount = (amount / 12.0).toFixed(1);
                }
                $(before_age_year).attr('disabled', false);
            }
            if (type_start_age === "year") {
                $(before_age_year).attr('disabled', true);
                $(before_age_year).val(type_start_age);
            }

            const index = data_entitlement_operation.findIndex(item => item.id === id);
            data_entitlement_operation[index].start_age = amount;
            console.log(data_entitlement_operation);
        }

        function EntitlementOperationBeforeAgeAmountChanged(id) {
            let tag = "#entitlement_operation_before_age_amount" + id;
            let tag_year = "#entitlement_operation_before_age_select" + id;
            let type = $(tag_year).val();
            let amount = parseInt($(tag).val());

            if (type === "month") {
                amount = (amount / 12.0).toFixed(1);
            }
            const index = data_entitlement_operation.findIndex(item => item.id === id);
            data_entitlement_operation[index].before_age = amount;

            console.log(data_entitlement_operation);
        }

        function EntitlementOperationBeforeAgeSelectChanged(id) {
            let tag = "#entitlement_operation_before_age_amount" + id;
            let before_age_year = "#entitlement_operation_before_age_select" + id;
            let type_before_age = $(before_age_year).val();
            let amount = parseInt($(tag).val());

            if (type_before_age === "month") {
                amount = (amount / 12.0).toFixed(1);
            }

            const index = data_entitlement_operation.findIndex(item => item.id === id);
            data_entitlement_operation[index].before_age = amount;
            console.log(data_entitlement_operation);
        }

        function EntitlementOperationDayAmountChanged(id) {
            let tag = "#entitlement_operation_day_amount" + id;
            let amount = parseInt($(tag).val());
            const index = data_entitlement_operation.findIndex(item => item.id === id);
            data_entitlement_operation[index].days = amount;
            console.log(data_entitlement_operation);
        }
        function EntitlementOperationHourAmountChanged(id) {
            let tag = "#entitlement_operation_hour_amount" + id;
            let amount = parseInt($(tag).val());
            const index = data_entitlement_operation.findIndex(item => item.id === id);
            data_entitlement_operation[index].hours = amount;
            console.log(data_entitlement_operation);
        }
    </script>
}
