<div class="modal fade" id="SpinModal" tabindex="-1" data-bs-backdrop="false" data-bs-keyboard="false" style="display: none;">
    <div class="modal-dialog modal-dialog-centered modal-md">
        <div class="modal-content border-0 shadow-lg" style="background: #ffffff; border-radius: 16px;">
            <div class="text-center py-5 px-4">
                <div class="lds-dual-ring big"></div>
                <div class="form-group row m-0 spinner-border text-success m-3" role="status">
                    <span class="sr-only text-center">Loading...</span>
                </div>
                <div class="mt-4">
                    <h4 class="mb-1 text-dark fw-bold">ระบบกำลังดำเนินการ</h4>
                    <p class="text-muted">กรุณารอสักครู่...</p>
                </div>
            </div>
        </div>
    </div>
</div>


<div id="SpinModalBackdrop"></div>

<style>

    .lds-dual-ring.big {
        width: 150px;
        height: 150px;
    }

        .lds-dual-ring.big:after {
            width: 90px;
            height: 90px;
            border-width: 9px;
            border-color: #4e73df transparent #4e73df transparent;
        }

    #SpinModal.show {
        display: flex !important;
        z-index: 2147483647 !important;
    }

    #SpinModalBackdrop {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(8px);
        z-index: 2147483646;
    }

        #SpinModalBackdrop.show {
            display: block !important;
        }
</style>